// ════════════════════════════════════════════════════════
//  MockIdProvider — ספק אימות מסמך זיהוי מדומה
//  ----------------------------------------------------------
//  מתנהג כאילו תמונת תעודת הזהות / הדרכון נקלטה ואומתה בהצלחה —
//  אבל *לא שומר את התמונה בשום מקום*: לא בדיסק, לא בזיכרון, ולא
//  בלוג. רק מאשר קבלה ומחזיר מזהה מדומה. נועד לפיתוח/הדגמה.
//
//  🔐 בפרודקשן זה יוחלף בספק אחסון מאובטח אמיתי — מוצפן, מוגבל
//     גישה, תואם חוק הגנת הפרטיות (PII), עם מדיניות שמירה/מחיקה.
//     ההחלפה נעשית במקום אחד בלבד (idverify/index.js), בדיוק כמו
//     שכבת התשלום (payments/) — שאר הקוד לא משתנה.
//
//  חשוב: verifyDocument לא זורק שגיאה בזרימה הרגילה — לכן שלב
//  הזיהוי לא ייכשל ולא יחזיר את האורח לתחילת הצ'ק אין.
// ════════════════════════════════════════════════════════
import { v4 as uuidv4 } from "uuid";
import { IdProvider } from "./IdProvider.js";

export class MockIdProvider extends IdProvider {
  async verifyDocument({ documentType, mediaUrl, contentType } = {}) {
    // ⚠️ דמו בלבד: התמונה (mediaUrl) מתקבלת ומיד נזרקת — שום הורדה,
    // שום שמירה לדיסק/זיכרון, ושום רישום של ה-URL או תוכן התמונה ללוג.
    // אנחנו רושמים רק את *סוג* המסמך ואת סוג-הקובץ הכללי, ללא כל PII.
    void mediaUrl; // לא נשמר, לא מורד, לא מתועד — בכוונה.
    console.log(
      `🪪 [MOCK ID] התקבל מסמך זיהוי (${documentType || "id"}, ${contentType || "image"}) — אומת, לא נשמר.`
    );
    return {
      success: true,
      documentId: `mock_id_${uuidv4()}`, // מזהה מדומה בלבד — לא מצביע על קובץ אמיתי
      documentType: documentType || "id",
      status: "verified",
    };
  }
}
