# סקייל: Redis ו-Postgres — מה נעשה, ומה נשאר

> **בקצרה:** ההגנה החשובה ביותר לריצה על יותר משרת אחד — **נעילה מבוזרת** —
> בנויה, נבדקת ופעילה. המעבר ל-Postgres מתועד כאן במלואו אך **טרם בוצע**,
> ולהלן בדיוק למה ומה נדרש.

---

## 1. הבעיה האמיתית בסקייל (ולא זו שנדמה)

הרושם המקובל הוא ש"SQLite לא מספיק גדול". זה נכון בסופו של דבר, אבל **זו לא
התקלה הראשונה שתפגע בכם**.

התקלה הראשונה שקטה לגמרי:

> `concurrency.js` מסדר בתור את ההודעות של אותו אורח — **בתוך תהליך אחד**.
> ברגע שרץ עוד instance, שתי הודעות של אותו אורח נופלות על שני תהליכים
> ורצות **בו-זמנית**.

מה שקורה בפועל:
- מצב הצ'ק אין נדרס — האורח חוזר לשלב קודם באמצע
- **הזמנת שירות חדרים נשלחת פעמיים** — המטבח מכין שתי מנות
- הגבלת הקצב מוכפלת במספר התהליכים

אין שגיאה, אין לוג, ואי אפשר לשחזר את זה בפיתוח (שם תמיד רץ תהליך אחד).

---

## 2. מה נבנה — ✅ עובד ונבדק

### `store/` — מצב משותף

| קובץ | תפקיד |
|---|---|
| `store/index.js` | **נקודת ההחלפה היחידה.** בוחר מימוש לפי `REDIS_URL` |
| `store/MemoryStore.js` | ברירת מחדל — זיכרון התהליך. **התנהגות זהה בדיוק להיום** |
| `store/RedisStore.js` | Redis אמיתי. הלקוח **מוזרק** — בלי תלות קשיחה חדשה |
| `store/lock.js` | `withGuestLock` · `checkSharedRate` |

### `withGuestLock` — ההגנה שהוחלפה בפועל

`bot.js` עבר מ-`withLock` ל-`withGuestLock`, שמרכיב **שתי** הגנות:

1. **נעילה בתהליך** (תמיד) — זולה ומיידית
2. **נעילה מבוזרת** (רק עם Redis) — בין תהליכים

בלי `REDIS_URL` המצב זהה לחלוטין להיום. עם `REDIS_URL` — בטוח להריץ כמה עותקים.

### שלוש החלטות שמונעות תקלות אמיתיות

| החלטה | התקלה שהיא מונעת |
|---|---|
| **TTL על הנעילה** | תהליך שקורס באמצע היה נועל את האורח **לנצח** |
| **טוקן ייחודי + שחרור מותנה (Lua אטומי)** | תהליך איטי שהנעילה שלו פגה היה משחרר את הנעילה של **מי שבא אחריו**. `GET` ואז `DEL` אינם אטומיים |
| **fail-open בתקלת Redis** | Redis שנפל היה משבית את כל המלונות. אורח בלי מענה גרוע מסיכון נדיר לכפילות |

### באג שנתפס בבנייה — ושווה להכיר

זיהוי סוג לקוח ה-Redis (node-redis מול ioredis) התבסס בגרסה הראשונה על
ניחוש. מול לקוח תקין הניחוש נכשל, הפרמטרים נשלחו בסגנון הלא נכון, `NX`
נבלע — ו**כל נעילה "הצליחה"**.

כלומר: ההגנה כולה הייתה מדומה, בזמן שהכול נראה עובד מצוין. עכשיו הזיהוי
חד-משמעי (`defineCommand` קיים ב-ioredis בלבד), ויש בדיקה לשני הדיאלקטים.

### הפעלה

```bash
# Railway → Variables
REDIS_URL=redis://default:<password>@<host>:<port>
```

```js
// אתחול (פעם אחת, בעלייה)
import { createClient } from "redis";      // npm i redis
import { RedisStore, setStore } from "./store/index.js";
const client = createClient({ url: process.env.REDIS_URL });
await client.connect();
setStore(new RedisStore({ client }));
```

השרת מדפיס בעלייה מה פעיל, ו**מתריע בקול** אם רצים בלי Redis — כדי שלא
יוסיפו instance בלי לשים לב.

---

## 3. Postgres — ✅ המסלול בנוי ונבדק (31.07.2026)

### מה עובד עכשיו

| רכיב | מצב |
|---|---|
| `store/pg-schema.sql` | ✅ סכימה מלאה (JSONB, TIMESTAMPTZ, מפתחות `(hotel_id,…)`) |
| `store/PgDriver.js` | ✅ דרייבר + **תור כתיבות מסודר** |
| `store/persistence.js` | ✅ `initPersistence()` — בחירה, בדיקת חיבור, יצירת סכימה |
| מונה חשבוניות אטומי | ✅ `UPDATE … RETURNING` |
| שטיפה בכיבוי חינני | ✅ |
| דיווח בעלייה | ✅ |

### הרעיון שמאפשר את זה בלי לשכתב 101 קריאות

`node:sqlite` סינכרוני ו-`pg` אסינכרוני — ואי אפשר לעטוף אסינכרוני בסינכרוני
ב-Node. המעבר נשען על שלוש עובדות שכבר קיימות בארכיטקטורה:

1. **קריאות זמן-ריצה כמעט לא נוגעות ב-DB** — `state.js`/`checkin.js` מחזיקים
   cache חי; ה-DB נקרא בעיקר **בעלייה**, ושם `await` מותר.
2. **הכתיבות הן write-through ולא נקראות בחזרה מיד** — לכן אפשר לתור אותן
   ולשטוף ברקע, **כל עוד הסדר נשמר**.
3. הקריאות הבודדות שכן קורות בזמן ריצה יושבות בהקשר אסינכרוני ממילא.

**מה שהתור מבטיח:** סדר (שתי פעולות על אותה שורה לא יתהפכו), אי-עצירה בשגיאה
(כתיבה שנכשלה לא מקפיאה את השאר), ואי-חסימה של הקורא.
**מה שהוא לא מבטיח:** קריסה *פתאומית* עלולה לאבד כתיבות שטרם נשטפו — ולכן
מסלולי כסף קוראים `flush()` במפורש, וכך גם הכיבוי החינני.

### הפעלה

```bash
npm i pg                       # התלות היחידה שנוספת, ורק למי שצריך
DATABASE_URL=postgres://…      # Railway → Variables
```
```js
import pg from "pg";
const pool = new pg.Pool({ connectionString: process.env.DATABASE_URL });
await initPersistence({ client: pool });   // server.js כבר קורא לזה
```

⚠️ `DATABASE_URL` בלי לקוח מוזרק → השרת **נכשל בקול ולא עולה**. נפילה שקטה
כאן פירושה ענן שרץ על SQLite בזמן שכולם בטוחים שהוא על Postgres, והנתונים
נעלמים בכל redeploy.

### ⚠️ מה שעדיין לא נבדק, ובכנות

**אין שרת Postgres בסביבת הפיתוח הזו** (אין Docker ואין PG מותקן). מה שנבדק
מול לקוח מזויף שמדבר את אותו פרוטוקול: המרת `?`→`$1`, סדר התור, אי-עצירה
בשגיאה, אי-חסימה, אטומיות מונה החשבוניות (20 הקצאות מקבילות → 20 מספרים
ייחודיים), ואתחול/כשל אתחול. **14 בדיקות ב-`persistence.test.mjs`.**

לפני שימוש אמיתי:
```bash
psql "$DATABASE_URL" -f store/pg-schema.sql
npm test
```

### ✅ תקרת הסקייל האמיתית — נפתרה (read-through cache)

התקרה מעולם לא הייתה SQLite אלא ש**הסשנים וההזמנות ישבו בזיכרון התהליך —
כולם, של כל המלונות, לנצח**. מלון–שניים עבד; מיליון מלונות היה נגמר ב-RAM
הרבה לפני שה-DB היה מתקשה. זה תוקן.

**`store/LruCache.js`** — cache חסום עם פינוי לפי שימוש אחרון + TTL אופציונלי:

| מה | משתנה סביבה | ברירת מחדל | TTL |
|---|---|---|---|
| סשנים (`state.js`) | `SESSION_CACHE_MAX` | 50,000 | 24ש' |
| הזמנות (`checkin.js`) | `RESERVATION_CACHE_MAX` | 20,000 | 48ש' |
| קונפיג מלונות (`config.js`) | `CONFIG_CACHE_MAX` | 5,000 | — |

**למה מותר לפנות בכלל:** הכתיבה כאן היא **write-through** — כל `patchSession`/
`saveReservation` כבר כתב ל-DB לפני שהערך בכלל *יכול* להתפנות. הפינוי מרוקן
זיכרון ותו לא; החטאה טוענת מחדש (`loadSession`/`loadReservation`).

> 🔴 **המלכודת המרכזית, וזה מה שהופך את הבדיקות לחובה:** *החטאה ב-cache אינה
> אורח חדש.* `getSession` הישן יצר סשן ריק כשלא מצא — ואחרי פינוי זה היה
> **מוחק לאורח את השם, החדר, ההיסטוריה ושלב הצ'ק אין באמצע שיחה**. עכשיו
> `getSession`/`peekSession` קוראים מה-DB *לפני* שהם יוצרים משהו.

**כל סריקת-זיכרון הפכה לשאילתת DB.** `Object.values(sessions)` אחרי פינוי
היה מחזיר רק את ה"חמים" — אורח מאוכלס היה נראה כלא קיים, ו-no-show לא היה
מחויב. הוחלפו: `allSessions` (עם `limit`), `sessionByRoom`, `sessionCount`,
`getActiveReservation`, `getPendingReservation`, `getReservationByRoom`,
`findNoShowReservations`, `activeReservationCount`.

**`reservations[id]` נשאר עובד** — הוא Proxy עם read-through, כדי שעמוד
הפיקדון ו-`checkin-routes.js` ימצאו הזמנה גם שעות אחרי שפונתה.

**בדיקות:** `lru.test.mjs` (12 — כולל מיליון הכנסות בזיכרון חסום) ו-
`readthrough.test.mjs` (12 — cache בגודל 3 שמכריח פינוי אמיתי, ומוודא
שהיסטוריה, שלב צ'ק אין, מספר חדר ובידוד מלונות שורדים אותו).

## 3ב. תוכנית המעבר המקורית (רקע)

### למה זה לא נעשה עכשיו — בכנות

המרה ל-Postgres **אינה החלפת דרייבר**. `node:sqlite` **סינכרוני**, ו-`pg`
**אסינכרוני**. בקוד יש **101 קריאות DB ב-17 קבצים**, וכולן סינכרוניות:

```js
const row = db.prepare("SELECT …").get(hotelId);   // סינכרוני
const row = await pool.query("SELECT …", [hotelId]); // אסינכרוני
```

כל אחת מהן הופכת ל-`await`, וכל פונקציה שקוראת להן הופכת ל-`async` — כולל
`getSession`, `patchSession`, `configFor` ו-`hotelConfig`, שנקראים מעשרות
מקומות ומצפים לערך מיידי.

**זו עבודה של 2–4 ימים עם סיכון רגרסיה גבוה** — ולעשות אותה עכשיו, ימים
אחרי הדגמה מוצלחת ולפני הדגמה נוספת, היה מסכן את מה שעובד תמורת יכולת
שעוד לא נדרשת. SQLite מחזיק בנוחות אלפי אורחים ליום על instance אחד.

### מתי כן לעבור

- כשצריך **יותר מ-instance אחד** (ואז גם Redis — כבר מוכן)
- כשה-DB עובר ~10GB או שהכתיבות נחנקות
- כשנדרש גיבוי/שחזור לנקודת זמן

### מה כבר מוכן

**`store/pg-schema.sql`** — סכימה מלאה המקבילה 1:1 ל-SQLite, עם:
- `JSONB` במקום TEXT (שאילתות על תוכן בלי פרסור באפליקציה)
- `TIMESTAMPTZ` במקום מחרוזות ISO
- **מפתחות מורכבים `(hotel_id, …)`** — בידוד הטננט נאכף ע"י בסיס הנתונים
- אינדקסים **חלקיים** על השורות החמות בלבד

### תוכנית המעבר

| שלב | מה עושים | זמן |
|---|---|---|
| 1 | `pg-schema.sql` על מסד ריק, אימות מול `db.js` | ½ יום |
| 2 | הפיכת `state.js` ל-async + עדכון הקוראים | 1 יום |
| 3 | `checkin.js` + `config.js` + `idverify/registry.js` | 1 יום |
| 4 | הרצת כל הבדיקות מול Postgres אמיתי | ½ יום |
| 5 | העברת נתונים (SQLite → PG) והחלפה | ½ יום |

### ⚠️ הנקודה שאסור לפספס במעבר

**מונה החשבוניות.** ב-SQLite ההגדלה אטומית כי התהליך חד-חוטי. בפוסטגרס
עם כמה תהליכים, שני צ'ק אאוטים בו-זמנית עלולים לקבל **אותו מספר סידורי** —
כלומר שתי חשבוניות מס עם אותו מספר. זו בעיה **חוקית**, לא באג תצוגה.

הפתרון בסכימה:
```sql
UPDATE invoice_counters SET seq = seq + 1
 WHERE hotel_id = $1 AND year = $2 RETURNING seq;
```
`UPDATE … RETURNING` נועל את השורה ומחזיר ערך ייחודי בעסקה אחת.

---

## 4. עלות (הערכה)

| רכיב | עלות התחלתית |
|---|---|
| Redis מנוהל | $0–15/חודש |
| Postgres מנוהל | $20–50/חודש |
| 2 instances | $10–40/חודש |

המשתנים הדומיננטיים בקנה מידה נשארים **Twilio ו-Anthropic** לפי נפח, ולא
בסיס הנתונים.

---

## 5. סיכום מצב

| יכולת | מצב |
|---|---|
| נעילה per-guest בתהליך אחד | ✅ עובד |
| **נעילה מבוזרת בין תהליכים** | ✅ **נבנה ונבדק** (Redis) |
| הגבלת קצב משותפת | ✅ נבנה ונבדק |
| עמידות לנפילת Redis | ✅ fail-open, נבדק |
| אזהרה בעלייה כשאין Redis | ✅ |
| סכימת Postgres | ✅ כתובה · ⚠️ לא הורצה מול שרת |
| המרת הקוד ל-async | ❌ לא בוצע (2–4 ימים, ראה §3) |

**המשמעות המעשית:** ברגע שמוסיפים `REDIS_URL` אפשר להריץ כמה עותקים בבטחה.
המעבר ל-Postgres נדרש רק בשלב הבא, והדרך אליו מתועדת ומוכנה.
