# StayBot — מוכנות למלונות אמיתיים (מה מוכן, מה דורש חיבור)

> תמונת מצב כנה ליום לפני ההדגמה: מה עובד *עכשיו* למלון אמיתי, ומה דורש
> חיבור/מפתח חיצוני. הכל מאחורי נקודות חיבור מוכנות — חיבור = מפתח או שורת
> קונפיג, בלי כתיבת קוד.

## ✅ מוכן עכשיו (עובד מלא)
- **צ'ק-אין/אאוט מלא** — שם, הזמנה, תאריכים, ת"ז (אימות+מחיקה), תנאי אירוח
  בני-אכיפה, פיקדון (כרטיס/מזומן), כרטיס/קוד דלת. חשבון מקובץ + חשבונית מס-קבלה.
- **קונסיירז' 24/7** — עברית+אנגלית, המלצות אמיתיות, רופא/מרקחת, לא ממציא.
- **ניתוב 7 מחלקות** — **וואטסאפ (אמיתי דרך Twilio) + מייל (אמיתי, ראה למטה)**.
- **חירום** — רפואי/אש/ביטחון/אזעקת טילים (מרחב מוגן), שני סוגי מלונות.
- **רום סרוויס + תפריטי מסעדות** · **הגעה/חניה** · **פרופיל אורח/VIP**.
- **מולטי-טננט** — בידוד מלא בין מלונות, קונפיג פר-`hotelId`.
- **מוכנות תפעולית** — graceful shutdown, `/ready`, דדופ הודעות, בדיקות שלמות
  אנשי קשר בעלייה, אזהרות אבטחה/מייל בעלייה.
- **חיבור מלון בקריאה אחת** — `POST /api/hotels {hotelId, number, config}`
  (רישום מספר + קונפיג + בדיקת אנשי קשר). מלון חדש = קריאה אחת, בלי קוד.

## 📧 מייל — מוכן, דורש מפתח אחד
עד היום המייל היה **Mock (רק לוג)**. עכשיו: `email/HttpEmailProvider.js` שולח
מייל **אמיתי** דרך Resend/SendGrid ברגע שמגדירים:
```
EMAIL_PROVIDER=resend           # או sendgrid
EMAIL_API_KEY=...               # מפתח ה-API
EMAIL_FROM="StayBot <alerts@yourhotel.com>"
```
בלי מפתח → נופל ל-Mock עם **אזהרה בעלייה** (וואטסאפ עדיין נשלח). שליחה שנכשלת
לא מפילה ניתוב מחלקה. **זמן חיבור: דקות** (חשבון Resend חינם + אימות דומיין).

## 🔌 דורש חיבור חיצוני (מוכן בקוד, נקודת החלפה אחת)
| מה | סטטוס | מה צריך | זמן |
|---|---|---|---|
| **WhatsApp אמיתי** | מספר Twilio מוגדר | אימות עסקי Meta + אישור שם | דקות–שעות (Meta עד ~30 יום) |
| **מייל אמיתי** | ✅ קוד מוכן | מפתח Resend/SendGrid + דומיין | דקות |
| **סליקה (פיקדון/חיוב)** | Mock | חשבון סוחר CardCom + מפתחות | לפי CardCom |
| **חשבונית לרשות המסים** | מסמך מלא, לא מוגש | ספק (Green Invoice/iCount) + מספר הקצאה כשנדרש | ימים |
| **PMS** | Mock (מאגר מובנה) | הסכם Optima (ישראל) / Apaleo | לפי הספק |
| **מוניות/מסעדות (הזמנה אמיתית)** | Mock (מעביר לאדם) | חשבון ספק | לפי הספק |
| **סקייל למיליונים** | תהליך בודד + SQLite | Postgres+Redis+תור | 2–4 ימי refactor |

## ⚠️ לפני go-live עם אורחים אמיתיים (checklist)
1. `DASHBOARD_PASSWORD` — סיסמה חזקה (לא ברירת המחדל).
2. `VALIDATE_TWILIO=true` + `BASE_URL` ציבורי (חוסם webhooks מזויפים).
3. `EMAIL_API_KEY` — אחרת המחלקות לא מקבלות מייל.
4. `ID_ENCRYPTION_KEY` — 32 בייט אמיתי (לא מפתח דמו).
5. אנשי קשר אמיתיים של 6 המחלקות (מספרים+מיילים) — לא הדמו.
6. נוסח תנאי אירוח מאושר יועמ"ש · תפריטים/מחירים אמיתיים · מרחב מוגן אמיתי.
7. סכום פיקדון אמיתי · מדיניות מע"מ תייר מול יועץ המס.

**השרת מזהיר בעלייה** על 1–3 אם לא הוגדרו — אי אפשר לפספס בטעות.

## הכנה מהירה למלון דמו (הדגמה)
כבר מוגדר: קמפינסקי (מלון מלא) + LALA (בוטיק, קוד דלת). הכל עובד עם Claude
אמיתי; WhatsApp/מייל/סליקה במצב דמו — מספיק להדגמה מלאה.
