# מדריך חיבור PMS — מה לבקש מכל מלון

> **מסמך זה נוצר אוטומטית מ-`pms/vendors.js`** (`node pms-guide.mjs`).
> אין לערוך אותו ידנית — כל שינוי יידרס. להוספת ספק: מוסיפים מפרט ב-`vendors.js`.
>
> נוצר עבור 13 מערכות PMS.

---

## מה זה PMS ולמה זה משנה

PMS הוא "מוח התפעול" של המלון — ההזמנות, החדרים, חשבון האורח וסטטוס הניקיון.
**בלי חיבור** הבוט עובד מצוין אבל על המאגר שלנו: הוא מקבל כל מספר הזמנה,
מקצה חדר קבוע, והחיובים לא מגיעים לחשבון האמיתי.
**עם חיבור** — ההזמנה נבדקת אמיתית, החדר מגיע מהמלון, והחיובים נרשמים לחשבון.

**מבחינת האורח כלום לא משתנה. מבחינת המלון — הכל מסתנכרן.**

---

## טבלת החלטה מהירה

| מערכת | איפה נפוצה | הרשמה עצמית | פרטים אומתו | קוד להגדרה |
|---|---|---|---|---|
| **אופטימה** | ישראל | ❌ | ✅ | `optima` |
| **אופרה / אורקל** | עולמי | ❌ | ✅ | `opera` |
| **מיוז** | אירופה · עולמי | ❌ | ✅ | `mews` |
| **קלאודבדס** | עולמי | ✅ | ✅ | `cloudbeds` |
| **אפאלאו** | אירופה | ✅ | ✅ | `apaleo` |
| **פרוטל** | אירופה | ❌ | ✅ | `protel` |
| **פידליו** | עולמי | ❌ | ⚠️ | `fidelio` |
| **גסטליין** | בריטניה | ❌ | ⚠️ | `guestline` |
| **רומרקון** | אירופה · עולמי | ❌ | ⚠️ | `roomraccoon` |
| **סטיינטאץ׳** | צפון אמריקה | ❌ | ⚠️ | `stayntouch` |
| **קלוק** | אירופה | ❌ | ⚠️ | `clock` |
| **הוטלוג׳יקס** | אסיה · עולמי | ❌ | ⚠️ | `hotelogix` |
| **איזי** | אסיה · עולמי | ❌ | ⚠️ | `ezee` |

**איך יודעים על מה המלון עובד?** פשוט שואלים: *"על איזו מערכת ניהול (PMS) אתם עובדים?"*
בישראל התשובה תהיה **אופטימה** ברוב המוחלט של המקרים.

---

## הכלל שחוסך זמן בכל פנייה

בכל מערכת יש הבדל בין **חיבור ערוצי מכירה** (Booking.com, Expedia) לבין
**ממשק תפעולי** (הזמנות וחשבון אורח). אלה מחלקות שונות אצל הספק.

> תמיד לומר: **"אנחנו צריכים ממשק תפעולי (PMS API) — לא חיבור ערוץ מכירה."**

בלי המשפט הזה מפנים אותך למחלקה הלא נכונה, וזה עולה שבועיים.

---

## שתי שאלות שקובעות את לוח הזמנים

1. **"האם ה-API נגיש מהאינטרנט, או רק מהרשת הפנימית של המלון?"**
   מערכות ותיקות (אופטימה מקומית, פידליו) מותקנות על שרת **בתוך המלון**.
   אז צריך פתיחת פורט או VPN — וזה שינוי תשתית שדורש את איש ה-IT.
   **זו סיבת העיכוב מספר 1.**

2. **"האם יש עלות אינטגרציה?"**
   לחלק מהספקים יש דמי שותף או דמי ממשק. עדיף לדעת מראש.

---

## מה עושים כשמשהו לא נתמך

לא כל מערכת מאפשרת הכל. הנפוץ ביותר: **רישום חיוב** (folio.post) שלא מאושר
לצד שלישי.

**המערכת בנויה לזה:** כל ספק מצהיר מה הוא יודע לעשות, והבוט **מדרדר בחן** —
אם אי אפשר לרשום חיוב אוטומטית, הבקשה עוברת לצוות כהתראה. שום דבר לא נשבר,
והאורח לא מרגיש.

---

## פירוט לפי מערכת

### אופטימה — Optima (Silverbyte / Priority)

| | |
|---|---|
| **מי מפתח** | סילברבייט, בבעלות פריוריטי |
| **איפה נפוץ** | ישראל |
| **למה זה חשוב** | מובילת השוק בישראל — 500+ מלונות, 85–90% מהשוק (דן, פתאל/לאונרדו, ישרוטל, אורכיד, פרימה, NYX) |
| **תיעוד** | <https://www.priority-software.com/hospitality-management/optima-pms/> |
| **הרשמה עצמית?** | ❌ לא — נדרשת פנייה דרך המלון/הספק |
| **פרטים אומתו?** | ✅ מול תיעוד הספק |

**איך משיגים גישה:**
דרך **המלון**, לנציג אופטימה שלו. אין פורטל מפתחים ואין הרשמה עצמית. לציין במפורש: ממשק **תפעולי** (PMS API) — *לא* GOC/Channel Manager.

> ⚠️ **לשים לב:** אצל מלונות רבים אופטימה מותקנת על שרת **בתוך המלון** — נדרש פתיחת פורט/VPN. זו סיבת העיכוב מס' 1 בישראל. רישום חיוב (folio.post) לצד שלישי לרוב מוגבל.

**מה לבקש מהמלון:**

| שדה | מה זה | | הערות |
|---|---|---|---|
| `baseUrl` | כתובת ה-API | **חובה** | דוגמה: `https://optima.hotel.co.il/api` · חייבת להיות נגישה מהאינטרנט — לא שם שרת פנימי |
| `apiUser` | שם משתמש | **חובה** | — |
| `apiPassword` | סיסמה 🔒 | **חובה** | — |
| `hotelCode` | קוד המלון במערכת | **חובה** | דוגמה: `H1` |
| `protocol` | סוג הממשק — rest או xml | רשות | דוגמה: `rest` |

**מה הבוט יוכל לעשות:** `checkin` · `checkout` · `folio.read` · `housekeeping.read` · `housekeeping.write` · `profile.read` · `reservation.read` · `reservation.search` · `room.assign`

> 💡 אין רישום חיוב אוטומטי אצל ספק זה — חיובי שירות חדרים יועברו לצוות במקום להירשם לחשבון. האורח לא מרגיש הבדל.

**להפעלה בקוד** (שורת קונפיג אחת, פר-מלון):

```js
updateConfigFor("<hotelId>", {
  pms_provider: "optima",
  pms_credentials: {
    baseUrl: "<כתובת ה-API>",
    apiUser: "<שם משתמש>",
    apiPassword: "<סיסמה>",
    hotelCode: "<קוד המלון במערכת>",
  },
});
```

📄 **מסמך מורחב:** `OPTIMA_PMS.md`

---

### אופרה / אורקל — Oracle OPERA Cloud (OHIP)

| | |
|---|---|
| **מי מפתח** | Oracle Hospitality |
| **איפה נפוץ** | עולמי |
| **למה זה חשוב** | תקן רשתות היוקרה הגדולות — קמפינסקי, הילטון, מריוט ואחרות. ה-PMS הנפוץ ביותר בעולם ברשתות גדולות |
| **תיעוד** | <https://docs.oracle.com/en/industries/hospitality/integration-platform/ohipu/> |
| **הרשמה עצמית?** | ❌ לא — נדרשת פנייה דרך המלון/הספק |
| **פרטים אומתו?** | ✅ מול תיעוד הספק |

**איך משיגים גישה:**
התהליך הכבד ביותר: נרשמים ל-OHIP דרך Oracle (Oracle Shop או הזמנה ידנית) → מקבלים גישה ל-**Partner Developer Portal** → מבצעים onboarding **לכל מלון בנפרד** → מקבלים Client ID, Client Secret ו-Application Key. בנוסף המלון יוצר **משתמש ממשק** ב-OPERA.

> ⚠️ **לשים לב:** צפי זמן: שבועות עד חודשים, ולרוב בעלות. נדרש תיאום עם ה-IT של הרשת ולא רק עם המלון הבודד. זה המסלול הרלוונטי לקמפינסקי אם הם על OPERA.

**מה לבקש מהמלון:**

| שדה | מה זה | | הערות |
|---|---|---|---|
| `baseUrl` | כתובת ה-OHIP של הרשת | **חובה** | דוגמה: `https://xxx.hospitality.oracleindustry.com` |
| `tokenUrl` | כתובת ה-OAuth של הרשת | **חובה** | — |
| `clientId` | Client ID | **חובה** | — |
| `clientSecret` | Client Secret 🔒 | **חובה** | — |
| `appKey` | Application Key (x-app-key) 🔒 | **חובה** | — |
| `hotelId` | מזהה המלון ב-OPERA | **חובה** | דוגמה: `TLVKM` |
| `interfaceUser` | משתמש הממשק (Integration User) | רשות | — |
| `interfacePassword` | סיסמת משתמש הממשק 🔒 | רשות | — |

**מה הבוט יוכל לעשות:** `checkin` · `checkout` · `folio.payment` · `folio.post` · `folio.read` · `housekeeping.read` · `housekeeping.write` · `profile.read` · `profile.write` · `reservation.read` · `reservation.search` · `room.assign` · `webhooks`

**להפעלה בקוד** (שורת קונפיג אחת, פר-מלון):

```js
updateConfigFor("<hotelId>", {
  pms_provider: "opera",
  pms_credentials: {
    baseUrl: "<כתובת ה-OHIP של הרשת>",
    tokenUrl: "<כתובת ה-OAuth של הרשת>",
    clientId: "<Client ID>",
    clientSecret: "<Client Secret>",
    appKey: "<Application Key (x-app-key)>",
    hotelId: "<מזהה המלון ב-OPERA>",
  },
});
```

---

### מיוז — Mews

| | |
|---|---|
| **מי מפתח** | Mews Systems (הולנד/צ׳כיה) |
| **איפה נפוץ** | אירופה · עולמי |
| **למה זה חשוב** | PMS ענן מוביל, נפוץ במלונות בוטיק ורשתות מודרניות |
| **תיעוד** | <https://docs.mews.com/connector-api/getting-started> |
| **הרשמה עצמית?** | ❌ לא — נדרשת פנייה דרך המלון/הספק |
| **פרטים אומתו?** | ✅ מול תיעוד הספק |

**איך משיגים גישה:**
פונים ל-Mews כשותף טכנולוגי. מקבלים **ClientToken** אחד לאפליקציה, ו**AccessToken נפרד לכל מלון** שמאשר את החיבור. יש סביבת דמו לפיתוח.

> ⚠️ **לשים לב:** נדרשת **הסמכה (certification)** של Mews לפני חיבור לסביבת הייצור. AccessToken הוא פר-מלון — לא לבלבל עם ClientToken.

**מה לבקש מהמלון:**

| שדה | מה זה | | הערות |
|---|---|---|---|
| `clientToken` | ClientToken — מזהה האפליקציה שלנו 🔒 | **חובה** | — |
| `accessToken` | AccessToken — מזהה **הנכס** (המלון) 🔒 | **חובה** | טוקן נפרד לכל מלון |
| `client` | Client — שם האפליקציה | **חובה** | דוגמה: `StayBot 1.0` |

**מה הבוט יוכל לעשות:** `checkin` · `checkout` · `folio.post` · `folio.read` · `housekeeping.read` · `profile.read` · `reservation.read` · `reservation.search` · `webhooks`

**להפעלה בקוד** (שורת קונפיג אחת, פר-מלון):

```js
updateConfigFor("<hotelId>", {
  pms_provider: "mews",
  pms_credentials: {
    clientToken: "<ClientToken — מזהה האפליקציה שלנו>",
    accessToken: "<AccessToken — מזהה **הנכס** (המלון)>",
    client: "<Client — שם האפליקציה>",
  },
});
```

---

### קלאודבדס — Cloudbeds

| | |
|---|---|
| **מי מפתח** | Cloudbeds (ארה״ב) |
| **איפה נפוץ** | עולמי |
| **למה זה חשוב** | נפוץ מאוד במלונות קטנים-בינוניים, הוסטלים ודירות נופש |
| **תיעוד** | <https://developers.cloudbeds.com/docs/authentication-1> |
| **הרשמה עצמית?** | ✅ כן — אפשר להתחיל לבד |
| **פרטים אומתו?** | ✅ מול תיעוד הספק |

**איך משיגים גישה:**
שתי דרכים: **API Key** (מומלץ) שהמלון מנפיק, או **OAuth 2.0** — שם צוות המלון מאשר את האפליקציה דרך Cloudbeds Marketplace. לרשת מלונות אפשר טוקן לכל הנכסים.

> ⚠️ **לשים לב:** ב-OAuth ה-redirect URI חייב להיות HTTPS. עדיף להנפיק משתמש ייעודי לאינטגרציה ולא להשתמש במשתמש של עובד — עובד שעוזב שובר את החיבור.

**מה לבקש מהמלון:**

| שדה | מה זה | | הערות |
|---|---|---|---|
| `apiKey` | API Key 🔒 | **חובה** | שיטת ההזדהות המומלצת של Cloudbeds |
| `propertyId` | Property ID | **חובה** | דוגמה: `123456` |

**מה הבוט יוכל לעשות:** `checkin` · `checkout` · `folio.post` · `folio.read` · `housekeeping.read` · `housekeeping.write` · `profile.read` · `reservation.read` · `reservation.search` · `webhooks`

**להפעלה בקוד** (שורת קונפיג אחת, פר-מלון):

```js
updateConfigFor("<hotelId>", {
  pms_provider: "cloudbeds",
  pms_credentials: {
    apiKey: "<API Key>",
    propertyId: "<Property ID>",
  },
});
```

---

### אפאלאו — Apaleo

| | |
|---|---|
| **מי מפתח** | Apaleo GmbH (גרמניה) |
| **איפה נפוץ** | אירופה |
| **למה זה חשוב** | PMS ענן API-first. חשבון מפתחים בהרשמה עצמית — ה-onboarding הקל ביותר, ולכן היעד הטוב ביותר לפיתוח ובדיקות |
| **תיעוד** | <https://apaleo.dev/> |
| **הרשמה עצמית?** | ✅ כן — אפשר להתחיל לבד |
| **פרטים אומתו?** | ✅ מול תיעוד הספק |

**איך משיגים גישה:**
**הרשמה עצמית** ב-apaleo.dev. יוצרים חשבון מפתחים, מקימים אפליקציה ומקבלים Client ID + Secret. המלון מאשר את החיבור לנכס שלו.

> ⚠️ **לשים לב:** נדרש scope מתאים לכל פעולה. חסר scope = 403 ולא הודעה ברורה.

**מה לבקש מהמלון:**

| שדה | מה זה | | הערות |
|---|---|---|---|
| `clientId` | Client ID | **חובה** | — |
| `clientSecret` | Client Secret 🔒 | **חובה** | — |
| `propertyId` | מזהה הנכס (Property ID) | **חובה** | דוגמה: `MUC` |

**מה הבוט יוכל לעשות:** `checkin` · `checkout` · `folio.payment` · `folio.post` · `folio.read` · `housekeeping.read` · `housekeeping.write` · `profile.read` · `profile.write` · `reservation.read` · `reservation.search` · `room.assign` · `webhooks`

**להפעלה בקוד** (שורת קונפיג אחת, פר-מלון):

```js
updateConfigFor("<hotelId>", {
  pms_provider: "apaleo",
  pms_credentials: {
    clientId: "<Client ID>",
    clientSecret: "<Client Secret>",
    propertyId: "<מזהה הנכס (Property ID)>",
  },
});
```

---

### פרוטל — protel (protel Air / I/O)

| | |
|---|---|
| **מי מפתח** | protel hotelsoftware GmbH (גרמניה, מקבוצת Planet) |
| **איפה נפוץ** | אירופה |
| **למה זה חשוב** | נפוץ בגרמניה ובאירופה. פלטפורמת אינטגרציה protel.I/O מבוססת תקני HTNG/OTA |
| **תיעוד** | <https://hub.protel.io/> |
| **הרשמה עצמית?** | ❌ לא — נדרשת פנייה דרך המלון/הספק |
| **פרטים אומתו?** | ✅ מול תיעוד הספק |

**איך משיגים גישה:**
נרשמים ב-**protel Marketplace** כשותף אינטגרציה, חותמים על NDA ומתארים את המקרה העסקי. לאחר האישור נפתחת גישה לפורטל המפתחים ולסביבת sandbox.

> ⚠️ **לשים לב:** פורטל סגור (partner-gated) — נדרש אישור מראש. הממשקים מבוססי HTNG/OTA עם תוספות משלהם.

**מה לבקש מהמלון:**

| שדה | מה זה | | הערות |
|---|---|---|---|
| `baseUrl` | כתובת ה-API | **חובה** | — |
| `apiKey` | מפתח / טוקן 🔒 | **חובה** | — |
| `hotelId` | מזהה המלון | **חובה** | — |

**מה הבוט יוכל לעשות:** `checkin` · `checkout` · `folio.post` · `folio.read` · `housekeeping.read` · `profile.read` · `reservation.read` · `reservation.search`

**להפעלה בקוד** (שורת קונפיג אחת, פר-מלון):

```js
updateConfigFor("<hotelId>", {
  pms_provider: "protel",
  pms_credentials: {
    baseUrl: "<כתובת ה-API>",
    apiKey: "<מפתח / טוקן>",
    hotelId: "<מזהה המלון>",
  },
});
```

---

### פידליו — Fidelio / OPERA on-premise

| | |
|---|---|
| **מי מפתח** | Oracle (לשעבר MICROS-Fidelio) |
| **איפה נפוץ** | עולמי |
| **למה זה חשוב** | הדור הוותיק של OPERA, מותקן בשרת בתוך המלון. עדיין נפוץ ברשתות ותיקות |
| **תיעוד** | <https://docs.oracle.com/en/industries/hospitality/> |
| **הרשמה עצמית?** | ❌ לא — נדרשת פנייה דרך המלון/הספק |
| **פרטים אומתו?** | ⚠️ המבנה המדויק מגיע עם מסמך הספק (הנתיבים ניתנים לדריסה) |

**איך משיגים גישה:**
דרך המלון ו-Oracle. בגרסאות on-prem החיבור נעשה לרוב דרך **OXI / ממשק HTNG** שמותקן אצל המלון, ולא דרך API ענן.

> ⚠️ **לשים לב:** כמעט תמיד **לא נגיש מהאינטרנט**. נדרש VPN, ולעיתים רכיב ממשק בתשלום. יכולות הכתיבה מוגבלות — לתכנן על קריאה בלבד.

**מה לבקש מהמלון:**

| שדה | מה זה | | הערות |
|---|---|---|---|
| `baseUrl` | כתובת שרת הממשק | **חובה** | בדרך כלל שרת בתוך המלון — נדרש VPN/פתיחת פורט |
| `apiUser` | שם משתמש | **חובה** | — |
| `apiPassword` | סיסמה 🔒 | **חובה** | — |
| `hotelId` | מזהה המלון | **חובה** | — |

**מה הבוט יוכל לעשות:** `folio.read` · `housekeeping.read` · `reservation.read` · `reservation.search`

> 💡 אין רישום חיוב אוטומטי אצל ספק זה — חיובי שירות חדרים יועברו לצוות במקום להירשם לחשבון. האורח לא מרגיש הבדל.

**להפעלה בקוד** (שורת קונפיג אחת, פר-מלון):

```js
updateConfigFor("<hotelId>", {
  pms_provider: "fidelio",
  pms_credentials: {
    baseUrl: "<כתובת שרת הממשק>",
    apiUser: "<שם משתמש>",
    apiPassword: "<סיסמה>",
    hotelId: "<מזהה המלון>",
  },
});
```

---

### גסטליין — Guestline

| | |
|---|---|
| **מי מפתח** | Guestline Ltd (בריטניה) |
| **איפה נפוץ** | בריטניה |
| **למה זה חשוב** | חזקה בבריטניה — מלונות עצמאיים, רשתות קטנות ודירות שירות |
| **תיעוד** | <https://www.guestline.com/> |
| **הרשמה עצמית?** | ❌ לא — נדרשת פנייה דרך המלון/הספק |
| **פרטים אומתו?** | ⚠️ המבנה המדויק מגיע עם מסמך הספק (הנתיבים ניתנים לדריסה) |

**איך משיגים גישה:**
דרך המלון, בפנייה לצוות האינטגרציות של Guestline. הממשקים מבוססי תקני HTNG/OTA.

> ⚠️ **לשים לב:** פרטי הממשק מגיעים עם המסמך שלהם — הנתיבים כאן ניתנים לדריסה דרך הקונפיג.

**מה לבקש מהמלון:**

| שדה | מה זה | | הערות |
|---|---|---|---|
| `baseUrl` | כתובת ה-API | **חובה** | — |
| `apiUser` | שם משתמש | **חובה** | — |
| `apiPassword` | סיסמה 🔒 | **חובה** | — |
| `siteId` | מזהה האתר/המלון | **חובה** | — |

**מה הבוט יוכל לעשות:** `folio.read` · `profile.read` · `reservation.read` · `reservation.search`

> 💡 אין רישום חיוב אוטומטי אצל ספק זה — חיובי שירות חדרים יועברו לצוות במקום להירשם לחשבון. האורח לא מרגיש הבדל.

**להפעלה בקוד** (שורת קונפיג אחת, פר-מלון):

```js
updateConfigFor("<hotelId>", {
  pms_provider: "guestline",
  pms_credentials: {
    baseUrl: "<כתובת ה-API>",
    apiUser: "<שם משתמש>",
    apiPassword: "<סיסמה>",
    siteId: "<מזהה האתר/המלון>",
  },
});
```

---

### רומרקון — RoomRaccoon

| | |
|---|---|
| **מי מפתח** | RoomRaccoon (הולנד) |
| **איפה נפוץ** | אירופה · עולמי |
| **למה זה חשוב** | All-in-one למלונות קטנים ובוטיק — PMS + Channel Manager + מנוע הזמנות |
| **תיעוד** | <https://roomraccoon.com/> |
| **הרשמה עצמית?** | ❌ לא — נדרשת פנייה דרך המלון/הספק |
| **פרטים אומתו?** | ⚠️ המבנה המדויק מגיע עם מסמך הספק (הנתיבים ניתנים לדריסה) |

**איך משיגים גישה:**
המלון מבקש גישת API מ-RoomRaccoon עבור ספק חיצוני. יש תיעוד API זמין לשותפים.

**מה לבקש מהמלון:**

| שדה | מה זה | | הערות |
|---|---|---|---|
| `baseUrl` | כתובת ה-API | **חובה** | — |
| `apiKey` | מפתח API 🔒 | **חובה** | — |
| `propertyId` | מזהה הנכס | **חובה** | — |

**מה הבוט יוכל לעשות:** `checkin` · `checkout` · `folio.post` · `folio.read` · `profile.read` · `reservation.read` · `reservation.search`

**להפעלה בקוד** (שורת קונפיג אחת, פר-מלון):

```js
updateConfigFor("<hotelId>", {
  pms_provider: "roomraccoon",
  pms_credentials: {
    baseUrl: "<כתובת ה-API>",
    apiKey: "<מפתח API>",
    propertyId: "<מזהה הנכס>",
  },
});
```

---

### סטיינטאץ׳ — StayNTouch

| | |
|---|---|
| **מי מפתח** | StayNTouch (ארה״ב) |
| **איפה נפוץ** | צפון אמריקה |
| **למה זה חשוב** | PMS ענן מוכוון מובייל, חזק בצ׳ק-אין עצמי |
| **תיעוד** | <https://www.stayntouch.com/> |
| **הרשמה עצמית?** | ❌ לא — נדרשת פנייה דרך המלון/הספק |
| **פרטים אומתו?** | ⚠️ המבנה המדויק מגיע עם מסמך הספק (הנתיבים ניתנים לדריסה) |

**איך משיגים גישה:**
דרך המלון, בבקשה לצוות האינטגרציות של StayNTouch.

**מה לבקש מהמלון:**

| שדה | מה זה | | הערות |
|---|---|---|---|
| `baseUrl` | כתובת ה-API | **חובה** | — |
| `apiKey` | מפתח API 🔒 | **חובה** | — |
| `hotelId` | מזהה המלון | **חובה** | — |

**מה הבוט יוכל לעשות:** `checkin` · `checkout` · `folio.read` · `housekeeping.read` · `profile.read` · `reservation.read` · `reservation.search` · `room.assign`

> 💡 אין רישום חיוב אוטומטי אצל ספק זה — חיובי שירות חדרים יועברו לצוות במקום להירשם לחשבון. האורח לא מרגיש הבדל.

**להפעלה בקוד** (שורת קונפיג אחת, פר-מלון):

```js
updateConfigFor("<hotelId>", {
  pms_provider: "stayntouch",
  pms_credentials: {
    baseUrl: "<כתובת ה-API>",
    apiKey: "<מפתח API>",
    hotelId: "<מזהה המלון>",
  },
});
```

---

### קלוק — Clock PMS+

| | |
|---|---|
| **מי מפתח** | Clock Software (בולגריה) |
| **איפה נפוץ** | אירופה |
| **למה זה חשוב** | PMS ענן עם API פתוח יחסית |
| **תיעוד** | <https://clock-software.com/> |
| **הרשמה עצמית?** | ❌ לא — נדרשת פנייה דרך המלון/הספק |
| **פרטים אומתו?** | ⚠️ המבנה המדויק מגיע עם מסמך הספק (הנתיבים ניתנים לדריסה) |

**איך משיגים גישה:**
המלון מפעיל את ה-API בהגדרות המערכת ומנפיק פרטי גישה.

**מה לבקש מהמלון:**

| שדה | מה זה | | הערות |
|---|---|---|---|
| `baseUrl` | כתובת ה-API | **חובה** | — |
| `apiUser` | שם משתמש | **חובה** | — |
| `apiPassword` | סיסמה 🔒 | **חובה** | — |
| `propertyId` | מזהה הנכס | **חובה** | — |

**מה הבוט יוכל לעשות:** `checkin` · `checkout` · `folio.post` · `folio.read` · `profile.read` · `reservation.read` · `reservation.search`

**להפעלה בקוד** (שורת קונפיג אחת, פר-מלון):

```js
updateConfigFor("<hotelId>", {
  pms_provider: "clock",
  pms_credentials: {
    baseUrl: "<כתובת ה-API>",
    apiUser: "<שם משתמש>",
    apiPassword: "<סיסמה>",
    propertyId: "<מזהה הנכס>",
  },
});
```

---

### הוטלוג׳יקס — Hotelogix

| | |
|---|---|
| **מי מפתח** | Hotelogix (הודו) |
| **איפה נפוץ** | אסיה · עולמי |
| **למה זה חשוב** | PMS ענן למלונות קטנים ובינוניים |
| **תיעוד** | <https://www.hotelogix.com/> |
| **הרשמה עצמית?** | ❌ לא — נדרשת פנייה דרך המלון/הספק |
| **פרטים אומתו?** | ⚠️ המבנה המדויק מגיע עם מסמך הספק (הנתיבים ניתנים לדריסה) |

**איך משיגים גישה:**
דרך המלון, בבקשה לתמיכת Hotelogix.

**מה לבקש מהמלון:**

| שדה | מה זה | | הערות |
|---|---|---|---|
| `baseUrl` | כתובת ה-API | **חובה** | — |
| `apiKey` | מפתח API 🔒 | **חובה** | — |
| `hotelId` | מזהה המלון | **חובה** | — |

**מה הבוט יוכל לעשות:** `folio.read` · `profile.read` · `reservation.read` · `reservation.search`

> 💡 אין רישום חיוב אוטומטי אצל ספק זה — חיובי שירות חדרים יועברו לצוות במקום להירשם לחשבון. האורח לא מרגיש הבדל.

**להפעלה בקוד** (שורת קונפיג אחת, פר-מלון):

```js
updateConfigFor("<hotelId>", {
  pms_provider: "hotelogix",
  pms_credentials: {
    baseUrl: "<כתובת ה-API>",
    apiKey: "<מפתח API>",
    hotelId: "<מזהה המלון>",
  },
});
```

---

### איזי — eZee / Yanolja Cloud

| | |
|---|---|
| **מי מפתח** | eZee Technosys (מקבוצת Yanolja) |
| **איפה נפוץ** | אסיה · עולמי |
| **למה זה חשוב** | נפוץ באסיה ובשווקים מתפתחים |
| **תיעוד** | <https://www.ezeetechnosys.com/> |
| **הרשמה עצמית?** | ❌ לא — נדרשת פנייה דרך המלון/הספק |
| **פרטים אומתו?** | ⚠️ המבנה המדויק מגיע עם מסמך הספק (הנתיבים ניתנים לדריסה) |

**איך משיגים גישה:**
דרך המלון, בבקשה לתמיכת eZee.

**מה לבקש מהמלון:**

| שדה | מה זה | | הערות |
|---|---|---|---|
| `baseUrl` | כתובת ה-API | **חובה** | — |
| `apiKey` | מפתח API 🔒 | **חובה** | — |
| `hotelCode` | קוד המלון | **חובה** | — |

**מה הבוט יוכל לעשות:** `folio.read` · `profile.read` · `reservation.read` · `reservation.search`

> 💡 אין רישום חיוב אוטומטי אצל ספק זה — חיובי שירות חדרים יועברו לצוות במקום להירשם לחשבון. האורח לא מרגיש הבדל.

**להפעלה בקוד** (שורת קונפיג אחת, פר-מלון):

```js
updateConfigFor("<hotelId>", {
  pms_provider: "ezee",
  pms_credentials: {
    baseUrl: "<כתובת ה-API>",
    apiKey: "<מפתח API>",
    hotelCode: "<קוד המלון>",
  },
});
```


---

## איך בודקים שהחיבור עובד

```js
import { pmsReadiness } from "./pms/index.js";
pmsReadiness("lala");
// → { vendor, ready, missing: [...], capabilities: [...] }
```

`missing` מפרט **בדיוק** אילו שדות עוד חסרים — זו הרשימה לשלוח למלון.

⚠️ מלון שהוגדר עם ספק אך חסרים לו פרטים **לא ישבור צ'ק אין**: המערכת נופלת
אוטומטית למאגר המובנה ומדפיסה אזהרה שמפרטת מה חסר.

---

## רב-מלונות

כל מלון והחיבור שלו, באותו הרגע ובאותו תהליך:

```
LALA        → optima      (אופטימה)
קמפינסקי    → opera       (אורקל)
מלון שלישי  → mock        (בלי PMS)
```

אין גלובל אחד. `pmsFor(hotelId)` בונה ספק נפרד לכל מלון עם ה-credentials
שלו בלבד — ואין מסלול שבו מלון אחד נוגע בנתונים של אחר.
