# פריסה לענן (Railway) — מה שחייב להיות נכון

> נכתב אחרי שהתגלה שהכתובת `hotel-bot-production.up.railway.app` מגישה
> **אפליקציה אחרת לגמרי** (עמוד סטטי של מלון בוורשה) ולא את הבוט: `/health`,
> `/ready` ו-`/api/routing` החזירו 404. כלומר הבוט לא היה פרוס שם.
> הקובץ הזה מרכז את כל מה שצריך כדי שפריסה תעבוד, ואיך לוודא זאת.

---

## 1. דרישת Node — חוסם פריסה אמיתי

בסיס הנתונים משתמש ב-**`node:sqlite`**, שיציב רק מ-**Node 24**. ב-Node 22 הוא
דורש דגל (`--experimental-sqlite`), וב-Node 18/20 הוא **לא קיים בכלל** —
והשרת קורס בעלייה עם `Cannot find module 'node:sqlite'`.

לכן `package.json` מצהיר:

```json
"engines": { "node": ">=24" }
```

Railway/Nixpacks מכבד את השדה הזה. **בלעדיו Railway עלול לבחור Node 20 והפריסה
תקרוס** — וזו קריסה שנראית כמו "האפליקציה לא עולה" בלי סיבה ברורה.

לחיזוק אפשר להוסיף ב-Railway גם משתנה `NIXPACKS_NODE_VERSION=24`.

---

## 2. משתני סביבה שחייבים להיות ב-Railway

| משתנה | חובה? | הערה |
|---|---|---|
| `ANTHROPIC_API_KEY` | ✅ | בלעדיו אין קונסיירז' AI |
| `TWILIO_ACCOUNT_SID` | ✅ | |
| `TWILIO_AUTH_TOKEN` | ✅ | |
| `TWILIO_WHATSAPP_NUMBER` | ✅ | `whatsapp:+14155238886`. **בלעדיו `DEMO_HOTEL` לא יוכל למפות את המספר** |
| `BASE_URL` | ✅ | כתובת ה-Railway עצמה (`https://<service>.up.railway.app`) — נכנסת לקישורי הפיקדון והחשבונית |
| `DASHBOARD_PASSWORD` | ✅ | סיסמה חזקה. שומרת על `/api/*` |
| `DEMO_HOTEL` | להדגמה | `lala` או `kempinski` — קובע את מלון ההדגמה בכל עלייה (§3) |
| `GOOGLE_PLACES_API_KEY` | מומלץ | בלעדיו הקונסיירז' רץ על מוק |
| `ID_ENCRYPTION_KEY` | מומלץ | 32 בייט hex. בלעדיו נגזר מפתח דמו עם אזהרה |
| `EMAIL_API_KEY` + `EMAIL_PROVIDER` + `EMAIL_FROM` | אופציונלי | בלעדיהם המייל למחלקות הוא מוק (וואטסאפ עדיין נשלח) |
| `PORT` | ❌ | **לא להגדיר** — Railway מזריק אותו לבד |

---

## 3. `DEMO_HOTEL` — למה זו הדרך היחידה שמחזיקה בענן

`demo-switch.mjs` כותב לבסיס הנתונים של **המכונה שמריצה אותו**. הענן הוא מכונה
אחרת עם DB אחר, ולכן החלפה מקומית **אינה משנה כלום בענן**.

ובנוסף: על Railway מערכת הקבצים **בת-חלוף** (אין volume כברירת מחדל), ולכן גם
הזרקה דרך `POST /api/hotels` נמחקת ב-redeploy הבא.

לכן ההגדרה נגזרת ממשתנה סביבה. בכל עליית שרת `demo-bootstrap.js`:

1. כותב את הקונפיג המלא של המלון (מ-`sample-hotels.mjs`) ל-DB.
2. ממפה את `TWILIO_WHATSAPP_NUMBER` לאותו מלון — **ורק** אליו.

אידמפוטנטי לחלוטין. **החלפת מלון בענן = שינוי `DEMO_HOTEL` + redeploy.**

⚠️ רץ רק כש-`DEMO_HOTEL` מוגדר, כדי שפריסה אמיתית (מלון לקוח שהוגדר דרך
`POST /api/hotels`) לא תידרס בכל restart.

---

## 4. אחרי הפריסה — איך לוודא שזה באמת עלה

### א. הלוג של Railway
בלוג הפריסה צריך להופיע:

```
🎬 DEMO_HOTEL=lala — מלון ההדגמה הוגדר בעלייה:
   +14155238886  →  לאלה בוטיק (lala)

📞 מספרים פעילים — מי עונה למי:
   +14155238886  →  לאלה בוטיק (lala)  ·  בוטיק  ·  קוד לדלת
```

אם רואים `kempinski` — או ש-`DEMO_HOTEL` חסר, או ש-`TWILIO_WHATSAPP_NUMBER` חסר.

### ב. בדיקה מרחוק מהמחשב

```
npm run cloud:check -- https://<service>.up.railway.app --expect=lala
```

הכלי מבחין במפורש בין שלוש התקלות שנראות זהות מבחוץ:

| תוצאה | מה זה אומר |
|---|---|
| `/health → 404` | **זה לא הבוט.** כתובת של שירות אחר, או שהפריסה נכשלה |
| `401` | סיסמת הדשבורד בענן שונה — להריץ עם `--token=<הסיסמה של Railway>` |
| `404` על `/api/tenant/resolve` | הענן מריץ **קוד ישן** — לפרוס מחדש |

### ג. Twilio
ב-Twilio Console → WhatsApp Sandbox → **"When a message comes in"** צריך להצביע
על `https://<service>.up.railway.app/webhook` (שיטה `POST`).

זה השלב שהכי קל לשכוח: אפשר שהענן יהיה מושלם, ובכל זאת אף הודעה לא תגיע אליו
כי ה-webhook עדיין מצביע על מנהרת ngrok כבויה.

---

## 5. מגבלה מודעת: SQLite על Railway בת-חלוף

בלי volume מחובר, כל redeploy מאפס סשנים והזמנות. להדגמה זה בסדר (ו-`DEMO_HOTEL`
משחזר את הגדרת המלון בכל עלייה), אבל **לא להריץ כך מול אורחים אמיתיים** — שם
נדרש Postgres מנוהל. ראה `SCALING.md`.

---

## 6. חלופה מהירה: הרצה מקומית + ngrok

אם צריך להיות באוויר עכשיו ובלי להתעסק בענן:

```
npm start                  # השרת עולה על :3000 וקורא את ההגדרה מ-hotel.db
ngrok http 3000            # בטרמינל נפרד
```

ואז ב-Twilio Sandbox להצביע על `https://<ngrok>/webhook`, ולעדכן `BASE_URL`
ב-`.env` לאותה כתובת (כדי שקישורי הפיקדון והחשבונית יעבדו).

במסלול הזה ההחלפה בין מלונות היא `npm run demo:lala` / `npm run demo:kempinski`,
והשרת נקלט מיד בלי restart.
