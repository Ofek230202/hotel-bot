# Secrets / environment
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Database (SQLite) — לא מעלים את מסד הנתונים ל-git
*.db
*.db-shm
*.db-wal

# מסמכי זיהוי של אורחים (PII רגיש) — אחסון דמו מקומי, לעולם לא ל-git
id-documents/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
